type Post @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String
  likes: String
  photo: String
  text: String
}

type Home @model @auth(rules: [{allow: public}]) {
  id: ID!
  address: String
  price: Int
  image_url: String
}
 